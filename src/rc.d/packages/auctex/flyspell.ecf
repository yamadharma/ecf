;;

(add-hook 
  'LaTeX-mode-hook
    (function 
      (lambda () (flyspell-mode 1))
    )
)

(add-hook 
  'LaTeX-mode-hook
    (function 
      (lambda () (setq ispell-parser 'tex))
    )
)

;(add-hook 
;  'LaTeX-mode-hook
;    (function 
;      (lambda () (local-unset-key "\M-\t"))
;    )
;)

;(add-hook 
;  'LaTeX-mode-hook
;    (function 
;      (lambda () (local-set-key "\M-\t" 'TeX-complete-symbol))
;    )
;)


;(define-key map "\e\t"   'TeX-complete-symbol) 



