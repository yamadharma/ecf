(desire 'font-lock)

(require 'lazy-lock)

(setopt font-lock-support-mode 'lazy-lock-mode)
; (lazy-lock-defer-contextually t)
(setopt font-lock-defer-on-scrolling nil) 
(setopt font-lock-defer-on-the-fly nil) 
(setopt lazy-lock-defer-time 0)         
(setopt lazy-lock-minimum-size 10240)   
