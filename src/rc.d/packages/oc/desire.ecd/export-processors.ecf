;;; -*- mode: emacs-lisp; lexical-binding: t; coding: utf-8-unix; -*-

(require 'ox)

(require 'oc-basic)
(require 'oc-bibtex)
(require 'oc-biblatex)
(require 'oc-natbib)

(setq org-cite-export-processors
      '(
	;; For LaTeX
	(latex biblatex)
	;; Fallback
	(t csl "gost-numeric.csl")))

;;;
