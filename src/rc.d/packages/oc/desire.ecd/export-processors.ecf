;;; -*- mode: emacs-lisp; lexical-binding: t; coding: utf-8-unix; -*-

(require 'oc-basic)
(require 'oc-biblatex)
(require 'oc-natbib)

(setq org-cite-export-processors
      '(
	;; For LaTeX
	(latex biblatex)					
	;; Fallback
	(t csl "gost-7.32-2017.csl")))

;;;
