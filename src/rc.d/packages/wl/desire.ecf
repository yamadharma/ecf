;;; Wanderlust -*- coding: utf-8-unix; -*-

;;(setopt wl-icon-dir "/usr/local/lib/emacs/etc")
;;{{{ Network

  ;;{{{ cache setting.

  ;; (messages in localdir, localnews, maildir are not cached.)
  (setopt elmo-archive-use-cache nil)
  (setopt elmo-nntp-use-cache t)
;  (setopt elmo-imap4-use-cache nil)
  (setopt elmo-imap4-use-cache t)
  (setopt elmo-pop3-use-cache t)

  ;;}}}

;;; Enable disconnected operation in IMAP folder.
(setopt elmo-enable-disconnected-operation t)

;;; Store draft message in queue folder if message is sent in unplugged status.
(setopt wl-draft-enable-queuing t)
;; when plug status is changed from unplugged to plugged,
;; queued message is flushed automatically.
(setopt wl-auto-flush-queue t)

;; offline at startup.
;(setopt wl-plugged nil)
;; change plug status by server or port at startup.
;(add-hook 'wl-make-plugged-hook
;	  '(lambda ()
;	     (elmo-set-plugged plugged(t/nil) server port)
;	     (elmo-set-plugged plugged(t/nil) server)
;	     ))

;;}}}

;; (setopt wl-nntp-posting-server "news4.fido7.ru")
;; (setopt wl-smtp-posting-server "mail.sci.pfu.edu.ru")

;; (setopt wl-local-domain "sci.pfu.edu.ru")

;; (setopt elmo-imap4-default-server "mail.sci.pfu.edu.ru")
;; (setopt elmo-imap4-force-login t)
;; (setopt elmo-nntp-default-server "news4.fido7.ru")

;;; Use modified UTF-7 (rfc2060) encoding for IMAP4 folder name
(setopt elmo-imap4-use-modified-utf7 t)

;;; Mark copied message as read
(setopt wl-fcc-force-as-read t)

;;;
