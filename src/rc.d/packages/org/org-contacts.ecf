;;; -*- mode: emacs-lisp; lexical-binding: t; coding: utf-8-unix; -*-
;;; 

(require 'org-contacts)

(setopt org-contacts-files (list (concat org-directory "personal/contacts.org")))

;;(setopt org-contacts-address-property "CITY")
;;(setopt org-contacts-birthday-property "BORN")
;;(setopt org-contacts-icon-property "PHOTOGRAPH")

;;;
