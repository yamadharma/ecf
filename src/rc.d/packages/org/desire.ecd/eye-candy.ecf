;;;

(setopt org-pretty-entities t)

;;; Turn on fontification for marked up text
(setopt org-fontify-emphasized-text t)

;;; Hide * and / in org text
(setopt org-hide-emphasis-markers t)

;;; Hyperlinks
(setopt org-link-descriptive t)

(setopt org-indirect-buffer-display 'current-window
      org-eldoc-breadcrumb-separator " → "
      org-enforce-todo-dependencies t
      org-entities-user
      '(("flat"  "\\flat" nil "" "" "266D" "♭")
        ("sharp" "\\sharp" nil "" "" "266F" "♯"))
      org-fontify-quote-and-verse-blocks t
      org-fontify-whole-heading-line t
      org-image-actual-width nil
      org-imenu-depth 8
      ;;; Sub-lists should have different bullets
      ;; org-list-demote-modify-bullet '(("+" . "-") ("-" . "+") ("*" . "+") ("1." . "a."))
      org-priority-faces
      '((?A . error)
        (?B . warning)
        (?C . success))
      org-tags-column 0)


;;; Toggle TeX-like syntax for sub- and superscripts
(setopt org-use-sub-superscripts '{})

;;;
