;;; The Zenburn colour theme ported to Emacs -*- coding: utf-8-unix; -*-
;; https://github.com/bbatsov/zenburn-emacs

;;; Scaled headings
;; Use the following settings before loading zenburn-theme

;; use variable-pitch fonts for some headings and titles
(setopt zenburn-use-variable-pitch t)

;; scale headings in org-mode
(setopt zenburn-scale-org-headlines t)

;; scale headings in outline-mode
(setopt zenburn-scale-outline-headlines t)

(load-theme 'zenburn t)

;;;
